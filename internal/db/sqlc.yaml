version: "2"

sql:
  - engine: "postgresql"
    queries: "../../sql/queries"
    schema: "../../sql/migrations"
    gen:
      go:
        package: "db"
        out: "."
        sql_package: "database/sql"
        emit_interface: true 