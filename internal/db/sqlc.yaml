version: "2"
sql:
  - engine: "postgresql"
    queries:
      - queries/members.sql
      - queries/settings.sql
    schema: migrations
    gen:
      go:
        package: "db"
        out: "."
        sql_package: "pgx/v5"